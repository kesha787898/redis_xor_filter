Main script is build_filters. It can be configured inside that script.

After scrip execution it is possible to draw plots in submodule plot_generators.

NOTE: plot_generators are AI generated scripts, however their correctness was double-checked by huu\man.