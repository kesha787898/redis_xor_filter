[project]
name = "reports"
version = "0.1.0"
description = ""
authors = [
    { name = "kesh787898", email = "kesh787898@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "redis (>=8.1.0,<9.0.0)",
    "tqdm (>=4.70.0,<5.0.0)",
    "pandas (>=3.0.5,<4.0.0)",
    "seaborn (>=0.13.2,<0.14.0)",
    "adjusttext (>=1.4.0,<2.0.0)",
]

[tool.poetry]
packages = [{ include = "reports", from = "src" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
